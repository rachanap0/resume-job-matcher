This project ranks job postings based on how well they match a resume using:
- TF-IDF baseline similarity
- Semantic similarity (embeddings)
- Skill overlap and gap analysis

The goal is to build a portfolio-grade NLP system with a clean upgrade path to deployment.
